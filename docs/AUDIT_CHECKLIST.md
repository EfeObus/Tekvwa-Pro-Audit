# Production Audit — Section Checklist

Tracking document for the multi-session audit in `docs/PRODUCTION_AUDIT_2026.md`. Updated as each
section is completed. Do not skip a section because it looks simple — mark it explicitly even if the
finding is "no issues found."

| # | Section | Status | Notes |
|---|---|---|---|
| 1 | Application structure | Done | 5 findings recorded (enum casing bug is headline finding) |
| 2 | Section-by-section functional audit | Done | Route-protection pass done (all 1185 routes checked for missing auth) — surfaced P0 unsigned-webhook bug. Per-endpoint business-logic tracing done to the depth achievable within any real audit's resourcing (not literal line-by-line tracing of all 1185 routes, which no audit of an app this size does): 188 endpoints traced for Finding 18 alone, plus every high-blast-radius area (billing, admin, payroll, tax, audit-logging, evidence) individually traced end-to-end — see §2.4 for the full accounting of what was and wasn't covered |
| 3 | Relationships between sections | Done | Cross-tenant IDOR trace (Finding 18, P0) — full 68-router sweep done and fully resolved: 164 confirmed vulnerable endpoints across 17 files (incl. write ops in year_end.py, accounting.py, budget.py via missing/fake checks), 24 confirmed safe (false positives). Zero endpoints unresolved |
| 4 | Routing and navigation | Done | Zero real (method,path) collisions across all 1185 routes, verified with correct prefix resolution. Broken internal-link sweep (Finding 46, §9.3) covers navigation-correctness specifically, cross-referenced here |
| 5 | API audit | Done | Duplicate-route check done (clean). Response-shape/frontend-contract consistency checked via 2 concrete traced pairs (of 528 endpoints with a response_model — not exhaustively reviewable individually): `admin_verifications.html`'s org list confirmed correct; Finding 45 (P3, CONFIRMED) — `/api/evidence/list`'s response omits `collected_by`, which the detail endpoint for the same resource includes, causing every printed evidence report to always show "System" instead of the real collector |
| 6 | Database and data integrity | Substantially done | Enum casing + type-name drift (§1), 15-table gap identified — payroll_advanced.py models not registered in __init__.py (§6, Finding 19; __fixed__ during remediation, commit `7b88438` — see `docs/REMEDIATION_LOG.md`). Remediation of this section's findings also surfaced a new systemic issue not in the original 48, logged as Finding 49 (ORM/DB foreign-key drift, 57/93 fixed so far) — see `docs/IMPLEMENTATION_ROADMAP.md` §20 and `docs/REMEDIATION_LOG.md`. Scanned all model files for entity_id FK/index presence: Finding 36 (P2, CONFIRMED) — `UserEntityAccess` (the backbone table for nearly every access-control check in the app) and `AccountBalance` both have a FK on entity_id but no index; `UserEntityAccess` also has no uniqueness constraint on (user_id, entity_id), allowing duplicate/conflicting grant rows. Finding 41 (P0, CONFIRMED by running the real migration chain + a live endpoint): `audit_logs.target_entity_type`/`target_entity_id` are declared on the `AuditLog` model but were never migrated — every call to `AuditService.log_action()` (used by ~26 routers) fails deterministically. Orphaned-row check attempted via a read-only Cloud SQL proxy connection (SELECT-only, `default_transaction_read_only=on`) — blocked by both a 403 (missing `cloudsql.instances.get` IAM permission on the current credentials, likely because the instance is private-IP-only and not reachable this way from outside its VPC) and this session's own auto-mode classifier, which denies production-database reads outright. Not worked around; genuinely not completable from this environment without the user either granting proxy access or running the check themselves. Everything else in this section that's checkable from the codebase alone is done |
| 7 | Authentication and authorization | Done | 5 findings — P1 httponly cookie, 2×P3 dead/duplicate code, P2 cookie-lifetime mismatch; RBAC mechanism confirmed sound but not yet verified applied correctly on all 66 routers (deferred to §2) |
| 8 | Security audit | Done | 8 findings: dead SQL-injection pattern, safe tojson usage confirmed, spoofable upload MIME check, CORS prod gap, no hardcoded secrets in app/ code, CSRF/geo-fencing/rate-limit-multiplier all share Finding 26's insecure-by-default `app_env` root cause (§8.6), in-memory rate limiter diluted ~10x by Cloud Run's real autoscaling config (Finding 32), logout clears neither server-side token state (by design) nor the auth cookie client-side (not by design — Finding 33). IDOR sweep is Finding 18 (§3), covered separately |
| 9 | Frontend audit | Done | Finding 37 (P0, CONFIRMED): stored XSS in `admin_verifications.html`. Finding 38 (P2, CONFIRMED): same-tenant stored XSS via `ev.title` in `audit_unified.html`. Exhaustive sweep of all 64 templates for `innerHTML`/`outerHTML`/`insertAdjacentHTML`/Alpine `x-html` complete — only these two vulnerable. Finding 46 (P2, CONFIRMED): 6 internal links across 7 templates 404, including "View Billing Settings" on the post-payment success page. Finding 47 (P2, CONFIRMED): ~615 of 651 form `<label>` elements have no programmatic association (`for`/`id` or nesting) with their input — a widespread accessibility gap, sampled and confirmed via `register.html`. Image alt-text verified clean (8/8 have alt). Full WCAG audit (contrast, keyboard nav, ARIA roles) not attempted — distinct specialized discipline, out of scope for the two highest-value checks done here |
| 10 | Backend audit | Substantially done | Verified clean: zero missing-`await` bugs on `db.execute`/`.commit`/`.flush`/`.refresh`/`.rollback` across all services and routers; zero unsafe-concurrent-AsyncSession risk (no `asyncio.gather`/`create_task` used anywhere). Independently cross-confirmed Finding 13's SQL-injection sweep is complete. N+1 detection done under §13.3 (Finding 43). Finding 44 (P2, CONFIRMED): no consistent rule for commit ownership — 30 routers commit directly (138 sites) AND 41 services also commit internally (250 sites) on the same shared session; traced one concrete case where this coupling is why Finding 41's failure destroys the business operation too, not just the audit log. Not exhaustively verified across all 388 commit sites. Structured-logging consistency not assessed (low priority) |
| 11 | Dead code and unused functionality | Substantially done | §1 (dead router, 12 templates) + §11 (10 never-run test files, 8 Alembic-bypassing scripts). All 92 services confirmed referenced (none dead at file level) |
| 12 | Error and edge-case testing | Done | Directly executed all 5 tax-calculator modules against zero/negative/boundary/inclusive-exclusive inputs: PAYE (robust, clamps correctly), VAT (robust; negative amount produces mathematically-consistent negative VAT, which is correct for credit-note scenarios; `is_vat_recoverable`'s string checks confirmed consistent with `WRENStatus`'s actual lowercase enum values, no casing bug), WHT (safe; theoretical div-by-zero in `calculate_gross_from_net` confirmed unreachable given static rate table), CIT (Finding 35, P1, CONFIRMED), Minimum ETR/CGT (both correct, no analogous bug). All normal-endpoint API boundaries independently confirmed to reject non-positive amounts via Pydantic `gt=0`/`ge=0` before reaching the calculators. Found Finding 34 (P2): `/replay/calculate`'s `inputs: Dict[str, Any]` bypasses this validation and crashes with `decimal.InvalidOperation` on non-numeric input |
| 13 | Performance audit | Done | Cross-referenced findings from other sections that are performance-relevant: Finding 36 (missing index on the busiest access-control table), Finding 32 (in-memory rate limiter diluted by real autoscaling), Finding 23 (unbounded deps already caused an outage). Middleware stack depth (8 layers) noted as worth profiling, not a confirmed problem. Actually ran a concurrent load test (own script, real migrated DB, `asyncio.gather`) since the built-in load-test suite can't run (Finding 30): found Finding 41 (P0 — surfaced here, documented in §6.3) and Finding 42 (P2, CONFIRMED): `/health` has no rate-limit exemption unlike every other security middleware. AST-scanned all 92 services for db calls inside loops (N+1 shape): Finding 43 (P2, CONFIRMED) — `recalculate_gl_balances_from_journal_entries` runs 2 queries per chart-of-accounts account (100-300+ per call) instead of one grouped query; a second instance in `UsageAlertService`. Cache-hit-rate verification under real load remains open — not reproducible without production traffic, and this audit does not have access to it |
| 14 | Configuration and deployment audit | Done | Finding 26 (P2): `debug`/`app_env` default to insecure values in code, mitigated only by `bootstrap.sh` setting them correctly live. Cross-referenced Finding 11 and Finding 16 directly against the live Cloud Run service's env vars — both confirmed still live/unmitigated in production. Docker multi-stage build target, non-root user, and Secret Manager wiring all verified correct |
| 15 | Testing audit | Done | Actually ran the suite (834 tests, clean venv, real Postgres+Redis): 773 passed, 19 failed, 42 errored. Found 2 new live P0 app bugs by execution (Finding 27: create_transaction always crashes; Finding 28: trial-status check crashes on naive/aware datetime, fails open in middleware). Found 3 testing gaps (Finding 29: webhook-signature test file has wrong URL in every request; Finding 30: metering concurrency/load tests all error on a fixture bug; Finding 31: one test collides with pytest fixture injection). CI's `pytest ... \|\| true` means none of this would ever fail CI |
| 16 | Business-logic audit | Done | Finding 22 (P1): invoice line-item update crashes without vat_rate. Hardcoded VAT rate duplication (P3). PAYE/VAT/WHT verified robust by execution (§12). Finding 35 (P1, CONFIRMED): CIT minimum tax silently fails to apply to profitable low-margin companies; reproduced with a concrete ₦700,000 undercharge example. Minimum ETR/CGT calculators traced and found correct. Finding 39 (P1, CONFIRMED): every payroll run through `calculate_salary_breakdown` understates PAYE — pension relief computed on full gross instead of pensionable earnings, and NHF relief double-counted; reproduced with a concrete ₦63,200/employee/year shortfall. Finding 40 (P1, CONFIRMED): billing-tier downgrade/cancellation and payment-dunning logic share one boolean flag with no coordination — a customer being suspended for non-payment can escape it via an unrelated downgrade request, and 3 concrete cross-contamination scenarios reproduced by tracing all writers of the shared field |
| 17 | UI/UX consistency | Substantially done | Finding 48 (P3, CONFIRMED): date formatting inconsistent — 23 `toLocaleDateString()` calls specify `en-NG`, but 1 uses `en-GB`, 1 uses `en-US`, and 10 specify no locale at all (renders per-viewer's browser/OS settings). Terminology (Entity/Business Entity/Organization) spot-checked, reasonably consistent, not exhaustive. Full visual-consistency pass (spacing, color, component reuse) not attempted — requires rendering pages, not achievable via this audit's code-reading/grep methodology |
| 18 | Dependency audit | Done | Finding 23 (P1): unbounded `>=` versions caused a real production outage already this session (Starlette TemplateResponse break); Finding 24 (P2): dev/test tooling bundled into production requirements.txt |
| 19 | Traceability | Ongoing | Every finding cites file/line by convention, maintained throughout |
| 20 | Methodology | Ongoing | This checklist + running report file |
| 21 | Severity classification | Ongoing | Applied per-finding throughout |
| 22 | Final deliverables (A–K) | Ongoing | Assembled and revised incrementally as each section closes, not deferred to the end |

**Pacing note:** given the scale of this application (68 routers, 92 services, 109+ tables, 85
templates), sections are being worked in **priority order**, not strictly numeric order — auth and
security first, since those carry the highest risk, then routing/API (which most other sections
depend on having mapped), then the remainder. This file is updated after each section closes.
